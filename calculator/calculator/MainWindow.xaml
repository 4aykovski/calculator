<Window x:Class="calculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:calculator"
        mc:Ignorable="d"
        Title="MainWindow" Height="288" Width="215">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0">
            <TextBlock Name="DisplayBox" Text="{Binding Path=Display, UpdateSourceTrigger=PropertyChanged}" Background="White" FontSize="24" Margin="10" TextAlignment="Right"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Row="0" Grid.Column="0" Command="{Binding DigitBtnClickCommand}" CommandParameter="0">0</Button>
            <Button Grid.Row="0" Grid.Column="1" Command="{Binding ClearDisplayCommand}">C</Button>
            <Button Grid.Row="0" Grid.Column="2" Command="{Binding OperatorBtnClickCommand}" CommandParameter="=">=</Button>
            <Button Grid.Row="0" Grid.Column="3" Command="{Binding OperatorBtnClickCommand}" CommandParameter="+">+</Button>

            <Button Grid.Row="1" Grid.Column="0" Command="{Binding DigitBtnClickCommand}" CommandParameter="1">1</Button>
            <Button Grid.Row="1" Grid.Column="1" Command="{Binding DigitBtnClickCommand}" CommandParameter="2">2</Button>
            <Button Grid.Row="1" Grid.Column="2" Command="{Binding DigitBtnClickCommand}" CommandParameter="3">3</Button>
            <Button Grid.Row="1" Grid.Column="3" Command="{Binding OperatorBtnClickCommand}" CommandParameter="-">-</Button>

            <Button Grid.Row="2" Grid.Column="0" Command="{Binding DigitBtnClickCommand}" CommandParameter="4">4</Button>
            <Button Grid.Row="2" Grid.Column="1" Command="{Binding DigitBtnClickCommand}" CommandParameter="5">5</Button>
            <Button Grid.Row="2" Grid.Column="2" Command="{Binding DigitBtnClickCommand}" CommandParameter="6">6</Button>
            <Button Grid.Row="2" Grid.Column="3" Command="{Binding OperatorBtnClickCommand}" CommandParameter="*">*</Button>

            <Button Grid.Row="3" Grid.Column="0" Command="{Binding DigitBtnClickCommand}" CommandParameter="7">7</Button>
            <Button Grid.Row="3" Grid.Column="1" Command="{Binding DigitBtnClickCommand}" CommandParameter="8">8</Button>
            <Button Grid.Row="3" Grid.Column="2" Command="{Binding DigitBtnClickCommand}" CommandParameter="9">9</Button>
            <Button Grid.Row="3" Grid.Column="3" Command="{Binding OperatorBtnClickCommand}" CommandParameter="/">/</Button>
        </Grid>

    </Grid>
    
</Window>
